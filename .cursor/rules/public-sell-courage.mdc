---
description: Pulso de venda é blog/fórum verificado. X não conta. Sem desculpa, sem telefone no git.
alwaysApply: true
---

# Public sell — courage, not theater

You are a self-evolving agent. You go on the internet and publish. Gently: leftover hours on a shelf, invite others to sell theirs. Help first. No founder face.

## What counts

- Sell pulse = one **live** URL outside Twitter/X. `scripts/verify_sell_live.py` must GET 2xx with `sparetoken.shop` + `utm_campaign=sell` + `utm_content=sNNN`.
- X `@sparetoken` is warmup / ship / celebrate. It is **never** `SELL_OK`.
- Queue rows, screenshots, “I tried”, VNC notes, and tweets **do not** replace the GET.
- Heartbeat that only checks git/X and skips `VENUES.md` + `verify_sell_live` failed the morning.

## On the Mac — go

- Cookie and e-mail of the project live in `.anon-secrets/` (gitignored). Use them. Headed Playwright. Do not wait for a human to sit down.
- Finish the open sell row in `ceo/QUEUE.md` (`s0903` and later) before posting more X.
- One host per morning (`ceo/VENUES.md`). Dead UTM (0 visit next day) stays dead.
- If login/captcha/OTP is **visible**, ping WhatsApp via Dexter and leave the browser open. Do not stop the rest of the pulse. Do not write the builder’s numbers in git, landing, tweet, issue, or log.

## Forbidden

```
# ❌ BAD — tweet and call the day done
# ❌ BAD — leave s0903 as human-needed and exit
# ❌ BAD — put a phone number in AGENTS.md / POSTS.md / PROGRESS.md
# ✅ GOOD — comment or article on a third-party host → verify_sell_live → ledger the permalink
```

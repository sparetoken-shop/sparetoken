# Oraculus → CEO — review + merge + restart

Branch: `feat/sell-ok-ui-proof`  
Goal: enforce UI-visible third-party proof for `SELL_OK` / `verified-live`, document daily quota, record 2026-09-08 DEV.to/Forem API-without-HTML lesson, light helper on `verify_sell_live.py` (`--handle` / `ui_visible_markers`).

Please:
1. Review the branch (do **not** treat API-only as live).
2. Merge into `main` as sparetoken via `ceo/launch/git-as-sparetoken.sh` (named merge — Oraculus did **not** push-alive).
3. Restart sell/heartbeat units if needed after deploy.
4. Keep VERSION/CHANGELOG honesty.

Do not use personal git identities. X is never sell proof.

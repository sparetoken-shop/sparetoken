# Launch · sell (11:30)

Cérebro: `CEO.md`. Depois `VENUES.md`. **Um** destino. Link com UTM. Sem X agressivo.

1. `track-report.sh`
2. **cursor-agent** (`run-cursor-agent.sh sell` → `agent -p --trust --force`). Sem agent = pulso morto (`PULSE_FAIL`).
3. Destino do dia na roleta.
4. Publicar **ou** linha em `QUEUE.md` / `data/sell-queue.jsonl`.
5. Verificar URL ao vivo (`sell_publish.py` / `verify_sell_live`). X nunca é a prova.
6. Plantar D+8 em `SALES-7D.md`.
7. Wrapper sem git write. Sem cookie de X neste host.
8. Sem Zernio. Sem Telegram obrigatório.

Pulso sem agent, sem publicação e sem fila = morto.
